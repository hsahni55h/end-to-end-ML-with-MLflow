# notebooks/01_wine_quality/

EDA for the wine_quality dataset — exploring the same UCI Wine Quality
(red) data already wired up in [`projects/01_wine_quality/`](../../projects/01_wine_quality/),
whose pipeline was built first as the `core/` framework's proof case. This
folder captures the exploration that pipeline's design decisions can be
checked against (and that future adjustments to it should draw on) rather
than the other way around.

## Contents

- `01_eda.ipynb` — shape/dtypes, missing values, target distribution,
  correlations, and outliers.

## Key findings

_To be filled in once the exploration is done._
